{
  "_about": [
    "Corrections to the vendored Smithy model, for shapes whose declared",
    "pattern is stricter than the service it describes. A corrected pattern",
    "keeps the member validated against what AWS really returns; the",
    "alternative, listing the field as an accepted violation, would stop",
    "checking the value at all.",
    "",
    "Each correction pins the exact upstream pattern it replaces. If a",
    "re-vendor changes that pattern, the loader fails rather than applying a",
    "correction to text it was never written against."
  ],
  "shapes": {
    "com.amazonaws.acm#Arn": {
      "_evidence": [
        "DescribeCertificate reports the issuing authority in",
        "Certificate.CertificateAuthorityArn, and a private certificate's",
        "authority is an AWS Private Certificate Authority resource, so the ARN",
        "carries the acm-pca service segment. The shape is the service's",
        "generic Arn, whose pattern fixes that segment to \"acm\", so no ARN AWS",
        "returns for that member can match it. The correction admits acm-pca",
        "alongside acm and changes nothing else."
      ],
      "pattern": {
        "replaces": "^arn:[\\w+=/,.@-]+:acm:[\\w+=/,.@-]*:[0-9]+:[\\w+=,.@-]+(/[\\w+=,.@-]+)*$",
        "with": "^arn:[\\w+=/,.@-]+:acm(-pca)?:[\\w+=/,.@-]*:[0-9]+:[\\w+=,.@-]+(/[\\w+=,.@-]+)*$"
      }
    }
  }
}
